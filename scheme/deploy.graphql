input DeployInput {
    versionId: String!
    environmentId: String!
}

type Deploy {
    id: ID!
    createdAt: Date!
    versionId: String!
    version: Version
    environmentId: String!
    environment: Environment
}