input DeployInput {
    versionId: String!
    environmentId: String!
}

type Deploy {
    id: ID!
    versionId: String!
    version: Version
    environmentId: String!
    environment: Environment
}