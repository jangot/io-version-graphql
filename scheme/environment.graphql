input EnvironmentInput {
    id: ID
    name: String!
    description: String!
    orderIndex: Int!
}

type Environment {
    id: ID!
    name: String!
    description: String!
    orderIndex: Int!
    rules: [Rule!]
    deploys: [Deploy!]
}